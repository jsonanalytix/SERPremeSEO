/**
 * Supabase exports
 * 
 * Re-exports all Supabase utilities for convenient imports.
 * 
 * Usage:
 *   import { createClient } from '@/lib/supabase/client' // Browser
 *   import { createClient, createAdminClient } from '@/lib/supabase/server' // Server
 *   import type { Lead, LeadInsert } from '@/lib/supabase/types'
 */

export * from './types'
